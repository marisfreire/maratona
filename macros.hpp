#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define el '\n'
#define step(begin,end) for(ll i = begin; i < end; i++)
#define vi vector<int>

int nxt(){
    int x; cin >> x;
    return x;
}