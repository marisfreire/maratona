#include "macros.hpp"

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // Meu update: mudar o salário
    // Meu query: contar o número de pessoas com salário entre [a,b]
    // Valor do nó: número de pessoas que tem aquele salário?
    
    return 0;
}